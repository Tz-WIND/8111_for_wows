# 8111 for WoWS -- local server dependencies.
#
# This project is managed with uv. The source of truth is ../pyproject.toml
# (+ ../uv.lock). To set up the environment, from the repo root run:
#
#     uv sync
#
# and launch with:
#
#     uv run python server/server.py --demo
#
# This requirements.txt is provided only for users who prefer plain pip:
#
#     python -m venv .venv && .venv\Scripts\activate
#     pip install -r server/requirements.txt
#     python server/server.py --demo

aiohttp>=3.10
